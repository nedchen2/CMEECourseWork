#include <stdio.h> 
     /* A standard library of C . 
     printf is one of the method in it */
int main (void) // the tap is not meaningful for C,same as R
     /* starting point of the exectuable part */
    {
        /* A comment.which you can put everywhere */
        printf("Hello, world of pain!\n");    // Another comment: all statements in C end with a
                                              // semicolon
    
        return 0;  // Everything went OK. Return 0 to the OS.
    }


// g++ hello.cpp
// cpp is formal for g++ (compilier for C++)
// c is a subset of c++
// gcc -Wall hello.c -o hello